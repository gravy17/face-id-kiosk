# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Async SQLite
sqlalchemy==2.0.30
aiosqlite==0.20.0

# Settings
pydantic==2.7.1
pydantic-settings==2.3.1

# JWT
python-jose[cryptography]==3.3.0

# Image processing
opencv-python-headless==4.9.0.80
numpy==1.26.4
Pillow==10.3.0

# Face ML — detection (SCRFD), recognition (ArcFace/w600k_mbf) and
# liveness (MiniFASNetV2) all run directly through onnxruntime now.
# No more deepface / retina-face / Silent-Face-Anti-Spoofing packages —
# see app/services/{retinaface,deepface,liveness}_service.py and app/ml/.
onnxruntime==1.27.0

# Rate limiting
slowapi==0.1.9

# Structured logging
structlog==24.2.0

# Multipart form parsing
python-multipart==0.0.9

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
